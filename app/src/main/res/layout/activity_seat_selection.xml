<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="#8B3A2F">

    <!-- Title -->
    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Selecciona tu asiento"
        android:gravity="center"
        android:textSize="20sp"
        android:textColor="#E07060"
        android:paddingTop="20dp"
        android:paddingBottom="8dp" />

    <!-- Screen bar -->
    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="◀━━━━━━━━━━━  ESCENARIO  ━━━━━━━━━━━▶"
        android:gravity="center"
        android:textSize="11sp"
        android:textColor="#ffcccc"
        android:paddingBottom="12dp"
        android:paddingTop="4dp" />

    <!-- Seat grid -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:layout_marginStart="12dp"
        android:layout_marginEnd="12dp"
        android:background="#6B2A1F"
        android:padding="8dp"
        android:orientation="vertical">

        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/recyclerSeats"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            tools:itemCount="80"
            tools:listitem="@layout/item_seat" />

    </LinearLayout>

    <!-- Legend -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center"
        android:paddingTop="10dp"
        android:paddingBottom="6dp">

        <View android:layout_width="14dp" android:layout_height="14dp"
            android:background="#4CAF50" android:layout_margin="4dp"/>
        <TextView android:layout_width="wrap_content" android:layout_height="wrap_content"
            android:text="Disponible" android:textColor="#ffdddd" android:textSize="11sp"
            android:layout_marginEnd="12dp"/>

        <View android:layout_width="14dp" android:layout_height="14dp"
            android:background="#E07060" android:layout_margin="4dp"/>
        <TextView android:layout_width="wrap_content" android:layout_height="wrap_content"
            android:text="Seleccionado" android:textColor="#ffdddd" android:textSize="11sp"
            android:layout_marginEnd="12dp"/>

        <View android:layout_width="14dp" android:layout_height="14dp"
            android:background="#3A0F0A" android:layout_margin="4dp"/>
        <TextView android:layout_width="wrap_content" android:layout_height="wrap_content"
            android:text="Vendido" android:textColor="#ffdddd" android:textSize="11sp"/>
    </LinearLayout>

    <!-- Buttons row -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:paddingStart="24dp"
        android:paddingEnd="24dp"
        android:paddingTop="4dp"
        android:paddingBottom="20dp"
        android:layout_marginTop="4dp">

        <!-- Back button -->
        <androidx.appcompat.widget.AppCompatButton
            android:id="@+id/buttonBack"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:layout_marginEnd="8dp"
            android:padding="14dp"
            android:text="VOLVER"
            android:textColor="#ffffff"
            android:textStyle="bold"
            android:textSize="15sp"
            android:background="@drawable/bg_confirm_button"
            android:stateListAnimator="@null"
            style="@style/Widget.AppCompat.Button" />

        <!-- Confirm button -->
        <androidx.appcompat.widget.AppCompatButton
            android:id="@+id/buttonConfirm"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="2"
            android:padding="14dp"
            android:text="CONFIRMAR ASIENTOS"
            android:textColor="#ffffff"
            android:textStyle="bold"
            android:textSize="15sp"
            android:background="@drawable/bg_confirm_button"
            android:stateListAnimator="@null"
            style="@style/Widget.AppCompat.Button" />

    </LinearLayout>

</LinearLayout>